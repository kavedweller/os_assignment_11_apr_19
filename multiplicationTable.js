
function multiplicationTable(num) {
    for (var i = 1; i <= 10; i++) {
        console.log(num + " × " + i + " = " + i * num);
    }
}

// multiplicationTable(7);
// console.log('\n');
// multiplicationTable(99);